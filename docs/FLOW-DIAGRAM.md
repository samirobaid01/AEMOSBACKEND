# AEMOS Backend Flow Diagrams

## 1. Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            CLIENT LAYER                                 │
│                                                                         │
│  📱 Mobile App    🌐 Web Client    🔌 IoT Device    📡 Sensor          │
│     (JWT)          (JWT)           (Token)         (Token)             │
└────┬────────────────┬────────────────┬──────────────┬──────────────────┘
     │                │                │              │
     │ HTTP           │ WebSocket      │ MQTT         │ CoAP
     │                │                │              │
     ▼                ▼                ▼              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         MAIN SERVER PROCESS(ES)                         │
│  Port: 3000                                                             │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Protocol Layer                                                  │   │
│  │  ├─ Express (HTTP)                                               │   │
│  │  ├─ Socket.IO (WebSocket)                                        │   │
│  │  ├─ Aedes (MQTT)                                                 │   │
│  │  └─ node-coap (CoAP)                                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Authentication Middleware                                       │   │
│  │  ├─ authMiddleware (JWT for users)                              │   │
│  │  └─ deviceAuth (Device tokens for IoT)                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Message Routing                                                 │   │
│  │  ├─ dataStreamController (HTTP)                                 │   │
│  │  ├─ messageRouter (MQTT/CoAP)                                   │   │
│  │  └─ deviceStateController                                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Data Persistence                                                │   │
│  │  └─ DataStream.create() → MySQL                                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Event Emission                                                  │   │
│  │  └─ RuleEngineEventBus.emit()                                   │   │
│  │     ↓                                                            │   │
│  │     └─ BullMQ (enqueue job to Redis)                            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             │ HTTP 201 Created (response)               │
│                             ↓                                           │
│                        [Client]                                         │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Notification Subscriber                                         │   │
│  │  └─ notificationBridgeService (Redis Pub/Sub subscriber)        │   │
│  │     ↓                                                            │   │
│  │     └─ socketManager.broadcastToRoom()                          │   │
│  │     └─ mqttPublisher.publish()                                  │   │
│  │     └─ coapPublisher.notifyObservers()                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
└─────────────────────────────┼───────────────────────────────────────────┘
                              │
                              │ (Notifications)
                              ▼
                        [Clients receive]
                              
┌─────────────────────────────────────────────────────────────────────────┐
│                             REDIS                                       │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  BullMQ Queue: "rule-engine-events"                             │   │
│  │  Jobs: { eventType, payload, enqueuedAt }                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Pub/Sub Channel: "notifications:device-state-change"           │   │
│  │  Messages: { type, event, notification, rooms }                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Index Cache: "rulechain:sensor:{uuid}"                         │   │
│  │  Values: [ruleChainId1, ruleChainId2, ...]                      │   │
│  │  TTL: 1 hour                                                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
└─────────────────────────────┼───────────────────────────────────────────┘
                              │
                              │ (Job pickup)
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      WORKER PROCESS(ES) x N                             │
│  Command: npm run worker                                                │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Initialization                                                  │   │
│  │  ├─ initModels() (Sequelize)                                    │   │
│  │  ├─ notificationBridge.initializePublisher()                    │   │
│  │  ├─ RuleEngineWorker.start()                                    │   │
│  │  └─ ScheduleManager.initialize()                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  BullMQ Worker                                                   │   │
│  │  Concurrency: 20 (configurable)                                 │   │
│  │  ├─ Picks up job from queue                                     │   │
│  │  └─ Calls processEvent(job)                                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Event Type Router                                               │   │
│  │  ├─ 'telemetry-data' → ruleChainService.trigger(sensorUUID)    │   │
│  │  └─ 'scheduled' → ruleChainService.execute(ruleChainId)        │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  RuleChainIndex.getRuleChainsForSensor(sensorUUID)              │   │
│  │  ├─ Check Redis cache (O(1))                                    │   │
│  │  │  Cache hit: Return [ruleChainIds]                            │   │
│  │  │  Cache miss: Query DB, build index, cache, return            │   │
│  │  └─ Returns: [1, 5, 12]                                         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  For each ruleChainId:                                           │   │
│  │  ├─ Fetch RuleChain + nodes from MySQL                          │   │
│  │  ├─ Collect sensor/device data (parallel)                       │   │
│  │  └─ Execute nodes:                                               │   │
│  │     1. Filter nodes (check conditions)                           │   │
│  │     2. Transform nodes (modify data)                             │   │
│  │     3. Action nodes (send notification, update state)            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│                             ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Action Node: Send Notification                                 │   │
│  │  └─ notificationManager.queueStateChangeNotification()          │   │
│  │     ↓                                                            │   │
│  │     Detects: No Socket.IO in worker                              │   │
│  │     ↓                                                            │   │
│  │     └─ notificationBridge.publish() → Redis                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                             │                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Scheduled Jobs (node-cron)                                     │   │
│  │  ├─ Load rule chains where scheduleEnabled=true                 │   │
│  │  ├─ Register cron jobs (e.g., '0 */5 * * * *')                  │   │
│  │  └─ On schedule: RuleEngineEventBus.emit('scheduled', ...)     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              │ (Notifications published to Redis)
                              ▼
                       [Main Server subscribes]
                              │
                              ▼
                    [Clients receive via Socket.IO/MQTT/CoAP]
```

---

## 2. Rule Engine Flow (Detailed)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  INGESTION                                                              │
└─────────────────────────────────────────────────────────────────────────┘

POST /api/v1/datastreams/token
{
  "value": "25.5",
  "variableName": "temperature",
  "recievedAt": "2026-01-20T10:30:00Z"
}
               │
               │ (deviceAuth middleware)
               ▼
    req.sensorId = 42
    req.deviceUuid = "abc-123"
               │
               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  dataStreamController.createDataStreamWithToken()                      │
└─────────────────────────────────────────────────────────────────────────┘
               │
               ├─ 1. Lookup telemetry: variableName → telemetryDataId
               │
               ├─ 2. INSERT INTO DataStream (value, telemetryDataId, recievedAt)
               │    → newDataStream (id: 9876)
               │
               ├─ 3. res.status(201).json({ data: newDataStream })
               │    [Response sent to client]
               │
               └─ 4. process.nextTick(async () => {
                      │
                      ├─ a. notificationManager.queueDataStreamNotification()
                      │     (Socket.IO notification for real-time UI)
                      │
                      └─ b. ruleEngineEventBus.emit('telemetry-data', {
                               sensorUUID: "sensor-abc-123",
                               dataStreamId: 9876,
                               telemetryDataId: 5,
                               recievedAt: "2026-01-20T10:30:00Z"
                            })
                            │
                            ▼
                      })

┌─────────────────────────────────────────────────────────────────────────┐
│  RuleEngineEventBus.emit()                                             │
└─────────────────────────────────────────────────────────────────────────┘
               │
               └─ ruleEngineQueue.add('telemetry-data', {
                    eventType: 'telemetry-data',
                    payload: { sensorUUID, dataStreamId, ... },
                    enqueuedAt: "2026-01-20T10:30:00.123Z"
                  })
                  │
                  ▼
               [BullMQ writes to Redis]
               [Job ID: 12345]

┌─────────────────────────────────────────────────────────────────────────┐
│  PROCESSING (Worker Process)                                           │
└─────────────────────────────────────────────────────────────────────────┘

Worker picks up job 12345 from Redis
               │
               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  RuleEngineWorker.processEvent(job)                                    │
└─────────────────────────────────────────────────────────────────────────┘
               │
               ├─ job.data.eventType = 'telemetry-data'
               ├─ job.data.payload.sensorUUID = "sensor-abc-123"
               │
               └─ Call: ruleChainService.trigger("sensor-abc-123")
                  │
                  ▼

┌─────────────────────────────────────────────────────────────────────────┐
│  ruleChainService.trigger(sensorUUID)                                  │
└─────────────────────────────────────────────────────────────────────────┘
               │
               ├─ 1. RuleChainIndex.getRuleChainsForSensor(sensorUUID)
               │    │
               │    ├─ Check Redis: "rulechain:sensor:sensor-abc-123"
               │    │  Cache hit: return [1, 5, 12]
               │    │  Cache miss:
               │    │    → SELECT * FROM ruleChainNode WHERE type='filter'
               │    │    → Loop through nodes, extract sensor UUIDs from config
               │    │    → Store in Redis with 1 hour TTL
               │    │    → return [1, 5, 12]
               │    │
               │    └─ Returns: [1, 5, 12]
               │
               └─ 2. For each ruleChainId in [1, 5, 12]:
                     │
                     ├─ Fetch RuleChain (id, name, nodes)
                     │  SELECT * FROM RuleChain WHERE id = 1
                     │  Include: RuleChainNode (filter → transform → action)
                     │
                     ├─ Collect context data (parallel):
                     │  ├─ _collectSensorData(sensorUUID)
                     │  └─ _collectDeviceData(deviceUUID)
                     │
                     └─ Execute rule chain:
                        │
                        ▼

┌─────────────────────────────────────────────────────────────────────────┐
│  Rule Chain Execution                                                  │
│  Chain: "High Temperature Alert"                                       │
└─────────────────────────────────────────────────────────────────────────┘

Node 1: Filter (type: 'filter')
  config: { sensorUUID: "sensor-abc-123", condition: "value > 25" }
  ├─ Current sensor data: { value: 25.5 }
  ├─ Evaluate: 25.5 > 25 → true
  └─ Result: PASS → Continue to next node
               │
               ▼
Node 2: Transform (type: 'transform')
  config: { action: "convertToFahrenheit" }
  ├─ Input: 25.5°C
  ├─ Calculate: (25.5 * 9/5) + 32 = 77.9°F
  └─ Output: { valueCelsius: 25.5, valueFahrenheit: 77.9 }
               │
               ▼
Node 3: Action (type: 'action')
  config: {
    action: "sendNotification",
    deviceStateId: 8,
    newStateValue: "warning"
  }
  ├─ 1. Update device state:
  │     INSERT INTO DeviceStateInstance (deviceStateId, value, timestamp)
  │     VALUES (8, "warning", NOW())
  │
  ├─ 2. Emit notification:
  │     notificationManager.queueStateChangeNotification({
  │       deviceUuid: "abc-123",
  │       deviceId: 42,
  │       stateName: "temperatureAlert",
  │       newValue: "warning",
  │       oldValue: "normal"
  │     })
  │     │
  │     ▼
  │  ┌───────────────────────────────────────────────────────────────┐
  │  │  notificationManager (Worker Process)                         │
  │  └───────────────────────────────────────────────────────────────┘
  │     │
  │     ├─ Detects: socketManager.getIo() → null (no Socket.IO)
  │     │
  │     └─ Fallback: notificationBridge.publish({
  │          type: 'socket',
  │          event: 'device-state-change',
  │          notification: {
  │            type: 'device-state-change',
  │            deviceUuid: 'abc-123',
  │            metadata: {
  │              stateName: 'temperatureAlert',
  │              newValue: 'warning',
  │              oldValue: 'normal'
  │            }
  │          },
  │          rooms: ['device-uuid-abc-123', 'device-42']
  │        })
  │        │
  │        └─ Redis PUBLISH to "notifications:device-state-change"
  │
  └─ 3. Also publish to MQTT and CoAP (if available)

┌─────────────────────────────────────────────────────────────────────────┐
│  NOTIFICATION DELIVERY                                                  │
└─────────────────────────────────────────────────────────────────────────┘

Redis Pub/Sub Channel: "notifications:device-state-change"
  │
  │ (Main server has subscriber listening)
  │
  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Main Server: notificationBridge subscriber                            │
└─────────────────────────────────────────────────────────────────────────┘
  │
  └─ Receives: { type: 'socket', event: 'device-state-change', ... }
     │
     ├─ if (broadcastAll)
     │    socketManager.broadcastToAll(event, notification)
     │
     └─ else
        rooms.forEach(room => {
          socketManager.broadcastToRoom(room, event, notification)
        })
        │
        ▼
     Socket.IO emit:
     io.to("device-uuid-abc-123").emit('device-state-change', {
       type: 'device-state-change',
       deviceUuid: 'abc-123',
       metadata: {
         stateName: 'temperatureAlert',
         newValue: 'warning',
         oldValue: 'normal'
       },
       timestamp: '2026-01-20T10:30:01.234Z'
     })
     │
     ▼
  📱 Client receives:
  socket.on('device-state-change', (data) => {
    console.log('Device state changed:', data);
    // Update UI
  })
```

---

## 3. Scheduled Jobs Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│  WORKER STARTUP                                                         │
└─────────────────────────────────────────────────────────────────────────┘

npm run worker
  │
  └─ startRuleEngineWorker.js
     │
     ├─ initModels()
     ├─ RuleEngineWorker.start()
     └─ ScheduleManager.initialize()
        │
        ▼

┌─────────────────────────────────────────────────────────────────────────┐
│  ScheduleManager.initialize()                                          │
└─────────────────────────────────────────────────────────────────────────┘
  │
  └─ 1. Load rule chains from database:
     │
     │  SELECT * FROM RuleChain WHERE scheduleEnabled = true
     │  
     │  Results:
     │  ┌────┬──────────────────────────┬─────────────────┬──────────┐
     │  │ id │ name                     │ cronExpression  │ timezone │
     │  ├────┼──────────────────────────┼─────────────────┼──────────┤
     │  │ 1  │ Hourly Temperature Check │ 0 0 * * * *     │ UTC      │
     │  │ 5  │ Daily Report Generator   │ 0 0 0 * * *     │ UTC      │
     │  │ 12 │ Every 5 Min Sensor Scan  │ 0 */5 * * * *   │ UTC      │
     │  └────┴──────────────────────────┴─────────────────┴──────────┘
     │
     └─ 2. For each rule chain, register cron job:
        │
        ├─ cron.schedule('0 0 * * * *', async () => {
        │    await RuleEngineEventBus.emit('scheduled', {
        │      ruleChainId: 1,
        │      organizationId: 100,
        │      cronExpression: '0 0 * * * *',
        │      timezone: 'UTC'
        │    })
        │  }, { timezone: 'UTC' })
        │
        ├─ cron.schedule('0 0 0 * * *', ...)
        │
        └─ cron.schedule('0 */5 * * * *', ...)
           │
           │ (Jobs are now scheduled)
           │
           ▼
        [Every 5 minutes, cron triggers]
           │
           ▼

┌─────────────────────────────────────────────────────────────────────────┐
│  Cron Job Fires (Every 5 minutes)                                      │
└─────────────────────────────────────────────────────────────────────────┘
  │
  └─ RuleEngineEventBus.emit('scheduled', {
       ruleChainId: 12,
       organizationId: 100
     })
     │
     ▼
  [BullMQ enqueues job to Redis]
     │
     ▼
  [Worker picks up job]
     │
     ▼

┌─────────────────────────────────────────────────────────────────────────┐
│  RuleEngineWorker.processEvent(job)                                    │
└─────────────────────────────────────────────────────────────────────────┘
  │
  ├─ job.data.eventType = 'scheduled'
  ├─ job.data.payload.ruleChainId = 12
  │
  └─ Call: ruleChainService.execute(12, {
       sensorData: [],
       deviceData: []
     })
     │
     ▼
  [Executes rule chain without specific sensor/device context]
  [Rule chain can fetch data from all sensors/devices]
     │
     ▼
  [Executes action nodes: e.g., generate report, send email]
     │
     ▼
  [Done]
```

---

## 4. Multi-Protocol Notification Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│  ACTION NODE TRIGGER                                                    │
│  (From rule chain execution in worker)                                  │
└─────────────────────────────────────────────────────────────────────────┘
                         │
                         ▼
    notificationManager.queueStateChangeNotification({
      deviceUuid: 'abc-123',
      stateName: 'temperature',
      newValue: '25.5',
      oldValue: '22.0'
    })
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  notificationManager (Worker Process)                                  │
└─────────────────────────────────────────────────────────────────────────┘
                         │
                         ├─ Construct notification object:
                         │  {
                         │    type: 'device-state-change',
                         │    deviceUuid: 'abc-123',
                         │    metadata: { stateName, newValue, oldValue }
                         │  }
                         │
                         └─ Emit to all protocols:
                            │
        ┌───────────────────┼───────────────────┬────────────────────┐
        │                   │                   │                    │
        ▼                   ▼                   ▼                    ▼
    Socket.IO             MQTT                CoAP             (Future: Push, Email)

┌────────────────────┐ ┌────────────────┐ ┌──────────────┐
│  Socket.IO Path    │ │   MQTT Path    │ │  CoAP Path   │
└────────────────────┘ └────────────────┘ └──────────────┘

socketManager.getIo()  mqttPublisher       coapPublisher
    │                  .publishNotification .notifyObservers
    ├─ null (worker)       │                     │
    │                      ▼                     ▼
    │                  Topic:                 Observers for
    │                  "aemos/devices/        device UUID
    │                   abc-123/               abc-123
    │                   notifications"           │
    │                      │                     │
    │                      └─ Payload:           └─ Payload:
    │                         { ... }               { ... }
    │                                               │
    ├─ Fallback:                                   │
    │  notificationBridge.publish({                │
    │    type: 'socket',                           │
    │    event: 'device-state-change',             │
    │    notification,                             │
    │    rooms: ['device-uuid-abc-123']            │
    │  })                                          │
    │    │                                         │
    │    ▼                                         │
    │  Redis PUBLISH                               │
    │  Channel: "notifications:device-state-change"│
    │    │                                         │
    │    └─────────┬───────────────────────────────┘
    │              │
    ▼              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  Main Server: Notification Bridge Subscriber                           │
└─────────────────────────────────────────────────────────────────────────┘
    │
    └─ Receives notification from Redis
       │
       ├─ Socket.IO:
       │  io.to('device-uuid-abc-123').emit('device-state-change', notification)
       │  │
       │  └─ 📱 Web/Mobile clients receive via WebSocket
       │
       ├─ MQTT:
       │  (Already sent directly from worker)
       │  │
       │  └─ 🔌 IoT devices subscribed to topic receive
       │
       └─ CoAP:
          (Already sent directly from worker)
          │
          └─ 📡 CoAP observers receive notification
```

---

## 5. Data Flow Timeline (Single Request)

```
Time    Process         Action                              Latency
────────────────────────────────────────────────────────────────────────
0ms     Client          POST /api/v1/datastreams/token     
                        { value: "25.5", variableName: "temperature" }
                        │
1ms     Main Server     ├─ deviceAuth middleware           1ms
        (Express)       │  └─ Validate device token
                        │     req.sensorId = 42
                        │
2ms                     ├─ dataStreamController            1ms
                        │  └─ Lookup telemetry by variableName
                        │
5ms                     ├─ MySQL INSERT                    3ms
                        │  └─ DataStream.create()
                        │     newDataStream.id = 9876
                        │
6ms                     ├─ Send HTTP 201 response          1ms
                        │  └─ res.json({ data: newDataStream })
                        │
                        │  [CLIENT RECEIVES RESPONSE]
                        │
7ms                     └─ process.nextTick(() => {        (async)
                           ├─ notificationManager (Socket.IO)
                           └─ RuleEngineEventBus.emit()
                        })
                        │
8ms     Redis           ├─ BullMQ write job                1ms
                        │  └─ Queue: "rule-engine-events"
                        │     Job ID: 12345
                        │
10ms    Worker          ├─ Pick up job 12345               2ms
        (BullMQ)        │  └─ processEvent(job)
                        │
12ms                    ├─ RuleChainIndex lookup           2ms
                        │  └─ Redis GET "rulechain:sensor:..."
                        │     (Cache hit)
                        │     Returns: [1, 5, 12]
                        │
15ms                    ├─ Fetch RuleChain #1              3ms
                        │  └─ MySQL SELECT + JOIN nodes
                        │
25ms                    ├─ Collect context data            10ms
                        │  ├─ _collectSensorData() (parallel)
                        │  └─ _collectDeviceData() (parallel)
                        │
35ms                    ├─ Execute filter node             10ms
                        │  └─ Evaluate: value > 25 → true
                        │
37ms                    ├─ Execute transform node          2ms
                        │  └─ Convert to Fahrenheit
                        │
42ms                    ├─ Execute action node             5ms
                        │  ├─ INSERT DeviceStateInstance
                        │  └─ notificationManager.queue()
                        │
43ms                    ├─ notificationBridge.publish()    1ms
                        │  └─ Redis PUBLISH to channel
                        │
45ms    Main Server     ├─ Redis subscriber receives       2ms
                        │  └─ notificationBridge handler
                        │
46ms                    └─ socketManager.emit()            1ms
                        │  └─ io.to('device-uuid-...').emit()
                        │
47ms    Client          📱 Socket.IO event received        1ms
                        socket.on('device-state-change', (data) => {
                          console.log('Temperature alert!', data);
                        })

────────────────────────────────────────────────────────────────────────
Total:  47ms (client receives notification)

Breakdown:
- Request → Response:         6ms  ✅ (Client not blocked)
- Rule execution:            36ms  ✅ (Asynchronous)
- Notification delivery:      5ms  ✅ (Fast)
```

---

## 6. Scaling Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    PRODUCTION DEPLOYMENT (10k req/s)                    │
└─────────────────────────────────────────────────────────────────────────┘

┌────────────────────┐
│  Load Balancer     │  (AWS ALB / Nginx)
│  (Round Robin)     │
└──────────┬─────────┘
           │
    ┌──────┴──────┬──────────────┐
    │             │              │
    ▼             ▼              ▼
┌────────┐    ┌────────┐    ┌────────┐
│ Main   │    │ Main   │    │ Main   │  (3 instances)
│ Server │    │ Server │    │ Server │  - Express
│   #1   │    │   #2   │    │   #3   │  - Socket.IO
└───┬────┘    └───┬────┘    └───┬────┘  - MQTT/CoAP
    │             │              │       - Notification subscriber
    │             │              │
    └─────────────┴──────────────┘
                  │
                  │ (Enqueue events, Subscribe to notifications)
                  │
                  ▼
        ┌──────────────────┐
        │   Redis          │  (Single instance / Sentinel)
        │   - BullMQ Queue │  - 4GB RAM
        │   - Pub/Sub      │  - Persistence: AOF
        │   - Index Cache  │  - Eviction: allkeys-lru
        └─────────┬────────┘
                  │
                  │ (Process jobs)
                  │
    ┌─────────────┼─────────────┬─────────────┬─────────────┐
    │             │             │             │             │
    ▼             ▼             ▼             ▼             ▼
┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
│ Worker │    │ Worker │    │ Worker │    │ Worker │    │ Worker │
│   #1   │    │   #2   │    │   #3   │    │   #4   │    │   #5   │
└───┬────┘    └───┬────┘    └───┬────┘    └───┬────┘    └───┬────┘
    │             │             │             │             │
    │  Concurrency: 20 jobs each                           │
    │  Total: 5 × 20 = 100 concurrent rule executions      │
    │                                                       │
    └───────────────────────────┬───────────────────────────┘
                                │
                                │ (Read/Write)
                                │
                                ▼
                    ┌────────────────────┐
                    │   MySQL Cluster    │
                    │   - 1 Master       │  (Read + Write)
                    │   - 2 Read Replicas│  (Read only)
                    └────────────────────┘

Capacity Estimate:
- HTTP Ingestion:     10,000 req/s
- Rule Processing:    8,000 rules/s
- Notification:       50,000 events/s
- MySQL Writes:       10,000 inserts/s
- MySQL Reads:        20,000 queries/s
- Redis Ops:          50,000 ops/s

Cost (AWS):
- EC2 (Main × 3):     $300/mo
- EC2 (Worker × 5):   $500/mo
- RDS MySQL:          $800/mo
- ElastiCache Redis:  $200/mo
- ALB + Data:         $100/mo
────────────────────────────────
Total:                $1,900/mo
```

---

## 7. Key Files Reference

| Component | File Path | Purpose |
|-----------|-----------|---------|
| **Event Bus** | `src/ruleEngine/core/RuleEngineEventBus.js` | Enqueues events to BullMQ |
| **Queue** | `src/ruleEngine/core/RuleEngineQueue.js` | BullMQ queue definition |
| **Worker** | `src/ruleEngine/core/RuleEngineWorker.js` | Processes jobs from queue |
| **Index** | `src/ruleEngine/indexing/RuleChainIndex.js` | Redis-backed sensor→ruleChain lookup |
| **Scheduler** | `src/ruleEngine/scheduling/ScheduleManager.js` | Cron-based scheduled jobs |
| **Rule Service** | `src/services/ruleChainService.js` | Executes rule chain logic |
| **Notifications** | `src/utils/notificationManager.js` | Buffers and emits notifications |
| **Bridge** | `src/services/notificationBridgeService.js` | Redis pub/sub for worker↔main |
| **Socket.IO** | `src/utils/socketManager.js` | WebSocket connection management |
| **Controller** | `src/controllers/dataStreamController.js` | HTTP telemetry ingestion |
| **Message Router** | `src/services/messageRouter.js` | MQTT/CoAP message routing |
| **Main Server** | `src/server.js` | Server initialization |
| **Worker Entry** | `src/workers/startRuleEngineWorker.js` | Worker process startup |

---

**Document Version**: 1.0  
**Last Updated**: 2026-01-20
